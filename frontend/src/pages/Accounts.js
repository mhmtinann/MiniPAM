import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Accounts() {
  return (
    <Box sx={{ color: '#fff' }}>
      <Typography variant="h4">Accounts</Typography>
      <Typography>Burada hesap yönetimi olacak.</Typography>
    </Box>
  );
}