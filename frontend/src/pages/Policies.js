import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Policies() {
  return (
    <Box sx={{ color: '#fff' }}>
      <Typography variant="h4">Policies</Typography>
      <Typography>Burada policy yönetimi olacak.</Typography>
    </Box>
  );
}